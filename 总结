{查看业务代码的总结}

从数据角度分析代码
	数据的分类：固定数据（常量），不定数据，无数据
	数据的来源：方法传入，新建的，静态属性，枚举值，本类方法，本类属性
	来源和分类：（一般情况）
		固定数据（枚举值）
		不定数据（方法传入）
		新建的，静态属性可能为固定数据和不定数据

业务流程以判断为基准，判断意味和流程出现分支

数据的处理
	数据封装

固定数据，无判断的业务流程，返回值总是相同的
固定数据，有判断的业务流程，是无意义的
不定数据，无判断的业务流程，返回类型相同，返回值由数据决定
不定数据，有判断的业务流程，返回类型不同（所以返回值也不同）

git使用http下载代码
	git config --global http.sslVerify false 
	env GIT_SSL_NO_VERIFY=true
